name: Offline Tooling

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  offline-tooling:
    name: Offline project tooling
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run phone-free validation tooling
        shell: pwsh
        run: .\tools\test_offline_project_tooling.ps1
